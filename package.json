{
  "name": "lunar-server",
  "version": "5.0.0",
  "private": true,
  "description": "Lunar backend \u2014 katalog MovieZone + jalur pemutar HLS (tanpa WebView)",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "setup": "npm install && npx playwright install chromium"
  },
  "dependencies": {
    "playwright": "^1.63.0"
  }
}
